[package]
name = "voluma-probe-tagless"
version = "0.1.0"
description = "Throwaway probe package: depends on a tagless valid-mars repo to exercise default-branch fallback."

[dependencies.voluma-probe-target]
url = "https://github.com/haowjy/voluma-probe-tagless-target"
version = ">=0.1.0, <1.0.0"
